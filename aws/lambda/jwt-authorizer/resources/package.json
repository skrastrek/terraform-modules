{
  "name": "lambda-jwt-authorizer",
  "version": "1.0.0",
  "main": "index.ts",
  "dependencies": {
    "aws-jwt-verify": "^4.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.141",
    "@types/node": "^20.14.10",
    "@aws-sdk/client-cognito-identity-provider": "^3.614.0",
    "esbuild": "^0.23.0"
  },
  "scripts": {
    "prebuild": "rm -rf nodejs/dist",
    "build": "nodejs/node_modules/.bin/esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js"
  }
}
